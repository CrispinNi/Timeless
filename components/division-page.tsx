import { activeServices } from "@/data/config"; import { ServiceCard } from "./service-card"; import Link from "next/link";
const content={memories:{eyebrow:"Interactive keepsakes",title:"Memories made together.",copy:"Turn the people, messages and emotions of your celebration into something you can keep long after the day is over.",steps:["Choose your experience","Tell us about your event","We personalize it","Make the memory"]},tailoring:{eyebrow:"Made-to-measure",title:"Made for you.",copy:"From the first idea to the final fitting, Timeless creates clothing around the individual, the occasion and the way it should feel.",steps:["Tell us what you need","Share your inspiration","Measurements & fitting","Final piece"]}};
export function DivisionPage({division}:{division:"memories"|"tailoring"}){const c=content[division];return <><section className="container-page grid gap-10 py-20 lg:grid-cols-2"><div className="self-center"><p className="eyebrow">{c.eyebrow}</p><h1 className="display mt-6">{c.title}</h1><p className="mt-8 max-w-xl leading-7 text-black/60">{c.copy}</p><Link className="btn btn-dark mt-9" href={`/book?division=${division}`}>Book / Order</Link></div><div className="media-placeholder"/></section><section className="bg-sand py-24"><div className="container-page"><p className="eyebrow">Explore services</p><div className="mt-10 grid gap-x-16 lg:grid-cols-2">{activeServices(division).map(s=><ServiceCard key={s.slug} service={s}/>)}</div></div></section><section className="container-page py-24"><h2 className="heading">How it works.</h2><div className="mt-12 grid gap-8 md:grid-cols-4">{c.steps.map((x,i)=><div className="border-t border-black/25 pt-4" key={x}><span className="text-xs">0{i+1}</span><p className="mt-5 font-serif text-2xl">{x}</p></div>)}</div></section></>}
