import Link from "next/link"; import { notFound } from "next/navigation"; import { services, formatPrice } from "@/data/config"; import { whatsappUrl } from "@/lib/whatsapp";
export function ServicePage({slug,division}:{slug:string;division:"memories"|"tailoring"}){const s=services.find(x=>x.slug===slug&&x.division===division&&x.available);if(!s)notFound();return <><section className="container-page grid gap-12 py-20 lg:grid-cols-2"><div><p className="eyebrow">Timeless {division}</p><h1 className="display mt-6">{s.name}</h1><p className="mt-8 max-w-xl leading-7 text-black/60">{s.longDescription}</p><p className="mt-6 text-xs uppercase tracking-widest">{formatPrice(s)}</p><div className="mt-9 flex flex-wrap gap-3"><Link href={`/book?division=${division}&service=${s.slug}`} className="btn btn-dark">Request this service</Link><a className="btn" href={whatsappUrl(`Hello Timeless, I'm interested in ${s.name}.`)}>Ask on WhatsApp</a></div></div><div className="media-placeholder"/></section>{s.customizationOptions.length>0&&<section className="bg-sand py-24"><div className="container-page"><p className="eyebrow">Personalized around you</p><h2 className="heading mt-5">Make it yours.</h2><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{s.customizationOptions.map(x=><p className="border-t border-black/25 py-5 font-serif text-2xl" key={x}>{x}</p>)}</div></div></section>}</>}
