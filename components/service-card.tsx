import Link from "next/link"; import { ArrowUpRight } from "lucide-react"; import type { Service } from "@/data/config"; import { formatPrice } from "@/data/config";
export function ServiceCard({service}:{service:Service}){return <Link href={`/${service.division}/${service.slug}`} className="group border-t border-black/20 py-7"><div className="flex items-start justify-between gap-6"><div><p className="font-serif text-3xl">{service.name}</p><p className="mt-2 max-w-lg text-sm leading-6 text-black/60">{service.shortDescription}</p><p className="mt-4 text-xs uppercase tracking-widest">{formatPrice(service)}</p></div><ArrowUpRight className="transition group-hover:translate-x-1 group-hover:-translate-y-1"/></div></Link>}
