"use client";
import Link from "next/link"; import { Menu, X } from "lucide-react"; import { useState } from "react";
const links = [["Memories","/memories"],["Tailoring","/tailoring"],["Our Work","/portfolio"],["About","/about"],["Contact","/contact"]];
export function Header(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-black/10 bg-ivory/95 backdrop-blur"><div className="container-page flex h-20 items-center justify-between"><Link href="/" className="font-serif text-2xl tracking-[.25em]">TIMELESS</Link><nav className="hidden items-center gap-7 md:flex">{links.map(([n,h])=><Link className="text-xs uppercase tracking-[.13em] hover:opacity-50" key={h} href={h}>{n}</Link>)}<Link className="btn btn-dark" href="/book">Book / Order</Link></nav><button className="md:hidden" aria-label="Toggle navigation" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="container-page flex flex-col gap-5 pb-7 md:hidden">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} key={h} href={h}>{n}</Link>)}<Link className="btn btn-dark" href="/book">Book / Order</Link></nav>}</header>}
