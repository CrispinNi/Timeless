"use client"; import { useState } from "react";
export function ContactForm(){const [message,setMessage]=useState("");async function send(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/enquiries",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...Object.fromEntries(f),division:"general"})});setMessage(r.ok?"Thank you. Your message has been received.":"We couldn't send this message. Please contact us on Instagram or WhatsApp.")}return <form onSubmit={send} className="mt-10 grid gap-6"><input className="field" required name="customer_name" placeholder="Your name"/><input className="field" required name="phone" placeholder="Phone / WhatsApp"/><input className="field" type="email" name="email" placeholder="Email"/><select className="field" name="reason"><option>General enquiry</option><option>Memories</option><option>Tailoring</option></select><textarea className="field" rows={4} required name="notes" placeholder="How can we help?"/><button className="btn btn-dark justify-self-start">Send message</button>{message&&<p role="status">{message}</p>}</form>}
