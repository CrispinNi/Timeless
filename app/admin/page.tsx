import { createClient } from "@supabase/supabase-js";
export const dynamic="force-dynamic"; export const metadata={title:"Admin"};
export default async function Page(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;let enquiries:any[]=[];if(url&&key){const result=await createClient(url,key,{auth:{persistSession:false}}).from("enquiries").select("*").order("created_at",{ascending:false}).limit(100);enquiries=result.data||[]}return <section className="container-page py-16"><p className="eyebrow">Management</p><h1 className="heading mt-5">Enquiries</h1>{!url||!key?<p className="mt-8 rounded bg-sand p-5">Connect Supabase to view enquiries.</p>:<div className="mt-10 overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead><tr className="border-b border-black/20"><th className="py-4">Reference</th><th>Name</th><th>Division</th><th>Service</th><th>Date</th><th>Status</th></tr></thead><tbody>{enquiries.map(e=><tr className="border-b border-black/10" key={e.id}><td className="py-4">{e.reference}</td><td>{e.customer_name}<br/><span className="text-black/50">{e.phone}</span></td><td>{e.division}</td><td>{e.service||"—"}</td><td>{e.event_date||"—"}</td><td>{e.status}</td></tr>)}</tbody></table>{!enquiries.length&&<p className="py-10 text-black/50">No enquiries yet.</p>}</div>}</section>}
